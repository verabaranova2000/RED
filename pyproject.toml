[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "refinement"
version = "0.1.0"
description = "Refinement pipeline tools"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]